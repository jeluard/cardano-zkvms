# SSH Deployment Configuration
# When using an SSH host alias (e.g., 'ovh'), leave SSH_USER empty
# and let ~/.ssh/config provide the User
# For direct hostname, set SSH_USER (e.g., 'ubuntu') 
# SSH_KEY_PATH: Optional explicit SSH key path (overrides ssh-agent/IdentityAgent)
SSH_USER=
SSH_HOST=example.com
SSH_KEY_PATH=~/.ssh/id_example
# IMPORTANT: REMOTE_PATH must be absolute (e.g. /home/ubuntu/cardano-zkvms)
# Do NOT use ~/cardano-zkvms as ~ is not expanded by systemd
REMOTE_PATH=/home/ubuntu/cardano-zkvms

# Remote OpenVM Configuration
# Paths relative to REMOTE_PATH
OPENVM_GUEST_DIR=crates/zkvms/openvm
OPENVM_STATIC_DIR=web/dist

# Port for the backend service to listen on (internal only)
BACKEND_PORT=8080

# Port for Caddy reverse proxy (frontend-facing)
CADDY_PORT=444

# Domain for the Caddy reverse proxy certificate and endpoint
CADDY_DOMAIN=example.com

# Web Frontend Configuration
# Backend URL to use when building for production
# IMPORTANT: With Caddy reverse proxy, use CADDY_DOMAIN:CADDY_PORT
# Examples:
#   http://localhost:8080          (local backend, no proxy)
#   https://api.example.com:444    (Caddy on custom port)
#   https://api.example.com        (Caddy with proxy on 443)
#   /                              (same-origin, static only, default)
BACKEND_URL=https://example.com:444
