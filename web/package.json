{
  "name": "openvm-uplc-verifier",
  "version": "0.1.0",
  "description": "OpenVM UPLC Verifier - Web UI with browser-local STARK verification",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.js",
    "build:prod": "NODE_ENV=production node esbuild.config.js",
    "dev": "node esbuild.config.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
